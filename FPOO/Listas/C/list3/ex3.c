#include <stdio.h>
int main(){
    int numero, i;
printf("Digite um numero inteiro");
    scanf("%d", &numero )

//For = o laço de repetiçao (loop)//
    for(i = 0; i <= numero; i++){
        printf("%d\n", i);
    }
        return 0;
}