#include <stdio.h>

int main(){
	int numero, e;
	
	printf("Insira um numero: ");
	scanf("%d", &numero);
	
	for(e = 0; i <= numero; e+=2){ // i+=2 tem intervalo entre os numeros de 2 em 2 
		printf("%d\n", e);
}
	return 0;
}